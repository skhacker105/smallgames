import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smallgames.app',
  appName: 'small-games',
  webDir: 'dist/small-games/browser'
};

export default config;
